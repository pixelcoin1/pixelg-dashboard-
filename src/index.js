import React from "react";
import ReactDOM from "react-dom/client";
import PixelGDashboard from "./PixelGDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<PixelGDashboard />);
